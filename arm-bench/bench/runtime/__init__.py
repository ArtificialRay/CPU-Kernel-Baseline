"""arm-bench runtime utilities: input generation, correctness, timing.

Renamed from `harness/` to avoid clashing with `solutions/<dataset>/_harness/`,
which is the on-disk C++ shim per dataset. This package is the Python-side
runtime support used by `bench/runner.py`.
"""
