*.zip
CLAUDE.md
*/build/
*.cmake
.venv/
.vscode/
.env
logs/
Guide.md
plot.ipynb
build/

# macOS
.DS_Store

# Claude Code session data
.codegraph/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Eval runtime outputs (generated at runtime, not checked in)
generations/*
results/*
baselines/*
traces/*
!generations/.gitkeep
!results/.gitkeep
!baselines/.gitkeep
!traces/.gitkeep

# SSH credentials — never commit these
eval/eval_config.json

# Large binary/analysis files
*.pdf
*.tar.gz

# Terraform — state contains live AWS resource info, providers are large binaries
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/.terraform.lock.hcl

.env
.mcp*
*.log

bench-trace/
__pycache__/
*_binding_templates/
agent-runs/
uv.lock
notebooks/

# Local clone of the HuggingFace arm-bench-trace dataset (not part of the repo)
arm-bench/

# Claude Code project settings (keep skills/ but not local config)
.claude/settings.json
.claude/settings.local.json
